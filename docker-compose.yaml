version: '1.0'
services:
  node:
    image: node
    container_name: my-node-app
    ports:
    - '3000:3000'
    depends_on:
    - "redis"
  redis:
    image: redis:latest
    container_name: redis_container
    ports:
    - '6379:6379'
    volumes:
    - ./redis_data:/var/lib/redis
